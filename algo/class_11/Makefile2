TARGET = bstsort
OBJS = main2.o buildBST.o tree.o data.o clock.o
CC = cc
CFLAGS = -Wall

.SUFFIXES: .c .o

${TARGET} : ${OBJS}
	${CC} ${CFLAGS} -o ${TARGET} $^

.c.o:
	${CC} ${CFLAGS} -c $<

clean:
	rm -f *.o
